# Legacy deployment guide (removed)

The previous **liquor multi-tenant SaaS** stack (CRA storefront, `/api/storefront/*`, AiSensy, VAPID, vendor dashboards) **has been removed** from this repository.

Deploy **Gharsip Custom Prints** end-to-end with **`DEPLOY_PRINTS.md`** (Next.js on Vercel + FastAPI on Render + Atlas).
