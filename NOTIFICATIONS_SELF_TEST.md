# Notifications self-test (archived)

The previous checklist covered **liquor SAAS** vendor PWA alerts (polling + Web Push + AiSensy). That stack **no longer exists** in this repo.

**Gharsip Custom Prints** today:

- **`/admin`** is a PIN-guarded dashboard that reads orders from **`/api/admin/backend-orders`** (proxied Mongo data).
- There is **no vendor PWA**, no Web Push, and no AiSensy hooks in-tree.

Reuse this filename later if you add WhatsApp templates or transactional email for Prints.
