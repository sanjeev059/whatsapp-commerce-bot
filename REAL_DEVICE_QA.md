# Real-device QA — archived liquor checklist

The URLs and credentials below applied to the **removed** CRA liquor storefront (`/store/sharma-wines`, vendor JWT admin, AiSensy, etc.). They are **not valid** for the current repo.

Use **`DEPLOY_PRINTS.md`** to exercise **Gharsip Custom Prints** on a phone instead (home → customize → cart → checkout → payment demo → `/admin` PIN).
