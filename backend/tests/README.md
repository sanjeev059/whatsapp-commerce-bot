# Backend tests

The old liquor SaaS pytest suite was removed when the codebase was narrowed to **Gharsip orders only**.

Add smoke tests locally with `pip install -r ../requirements-full.txt && pytest`, or keep API checks in CI against `GET /api/`.
